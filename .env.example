# Адрес бэкенда. Переменная задана — фронт ходит в реальный API, не задана — работает
# на мок-данных: демо должно открываться по ссылке без поднятого бэкенда с базой и MinIO.
#
# Локально бэкенд поднимается из монорепо: docker compose up backend
VITE_API_URL=http://localhost:8080
