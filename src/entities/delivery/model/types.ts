import type { AdminDelivery, AdminDeliveryStatus } from '@/shared/api/generated/model'

/**
 * Доставка вещи по собравшейся цепочке — то, что видит сотрудник ПВЗ.
 *
 * Своей модели и маппера у неё нет намеренно: экран показывает ровно состояние бэкенда.
 * У цепочки маппер оправдан — там пять наших состояний против четырёх контрактных и «я»,
 * которого в данных нет. Здесь ни того, ни другого: перевод типа один в один был бы
 * слоем, который нечего переводить.
 */
export type Delivery = AdminDelivery

/** Физический путь вещи: ждём в ПВЗ → приняли → повезли → отдали. */
export type DeliveryStatus = AdminDeliveryStatus
