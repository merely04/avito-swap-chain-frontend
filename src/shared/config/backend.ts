/**
 * Режим работы данных. Бэкенд подключается переменной `VITE_API_URL`: задана — фронт ходит
 * в реальный API, не задана — работает на мок-функциях.
 *
 * Флаг нужен не для удобства разработки, а ради демо: раздел должен открываться по ссылке
 * без поднятого бэкенда с базой и хранилищем — жюри проверяет продукт, а не окружение.
 */
export const isBackendConnected = Boolean(import.meta.env.VITE_API_URL)

/*
 * Чего в контракте (`api/openapi.yaml`, версия 0.9.0) нет — это работает на моках даже при
 * подключённом бэкенде. Список держим здесь, чтобы он не расползался по комментариям:
 *
 * - состояние вещи — поля нет в схеме `Item`;
 * - фото в правке объявления — `imageUrls` в `UpdateItemRequest` не принимается;
 * - отмена варианта из-за ушедшей вещи (`cancelled`) — статуса нет: конкурирующие цепочки
 *   бэкенд отклоняет как `REJECTED`, а причину кладёт в событие;
 * - служебный канал «Авито Обмен» в переписке — это пояснение интерфейса, а не разговор:
 *   отправитель сообщения в контракте всегда пользователь;
 * - подсказки желания — эндпоинта нет, и модели за ними тоже: это эвристика на поисках
 *   и избранном (`suggestWish`);
 * - жалоба на человека из его профиля — ручка есть, но она про сообщение чата: без
 *   `messageId` пожаловаться нельзя, а в профиле сообщения нет. Жалоба из переписки уехала
 *   на бэкенд, из профиля осталась на памяти вкладки.
 *
 * Распознавание фото из списка ушло: в 0.6.0 появились `POST /vision/analyze` и событие
 * `vision.analysis.completed` в потоке. Модель отдаёт описание, категорию и состояние —
 * названия среди них нет, его по-прежнему пишет человек.
 *
 * Уведомления из списка тоже ушли: в 0.6.0 есть `GET /notifications`, `POST /notifications/read`
 * и событие `notification.created` в потоке. На моках журнал остался — демо обязано
 * открываться по ссылке без сервера.
 *
 * Стадия передачи из списка ушла совсем: в 0.6.0 есть и `POST /chains/{id}/receipt`, и признак
 * `receiptConfirmed` у каждого участника — прогресс «N из M» считается по реальным данным.
 *
 * Категория ушла из списка 12 авг: появились `GET /categories` и `categoryId` у вещи, фронт
 * шлёт выбор человека в `POST /items` и `PATCH /items/{id}`. Выбор человека больше не затирается
 * анализом: у вещи два поля, `categoryId` — её владельца, `offerCategoryId` — модели.
 *
 * Желание списком тоже ушло: в контракте это `wishes`, массив вариантов, у каждого свои
 * идентификатор и категория. Требование шло со стороны клиента с первого дня — до этого
 * варианты склеивались в одну строку через « или » и разбирались обратно при чтении.
 *
 * Чёрный список ушёл из списка 14 авг: в 0.9.0 есть `GET/POST /blocks` и `DELETE /blocks/{id}`.
 * Блокировка перестала быть пометкой в браузере — бэкенд убирает пару из подбора в обе
 * стороны и распускает общие незавершённые цепочки.
 */
