/**
 * Режим работы данных. Бэкенд подключается переменной `VITE_API_URL`: задана — фронт ходит
 * в реальный API, не задана — работает на мок-функциях.
 *
 * Флаг нужен не для удобства разработки, а ради демо: раздел должен открываться по ссылке
 * без поднятого бэкенда с базой и хранилищем — жюри проверяет продукт, а не окружение.
 */
export const isBackendConnected = Boolean(import.meta.env.VITE_API_URL)

/**
 * Чего в контракте (`api/openapi.yaml`, версия 0.6.0) пока нет — это работает на моках
 * даже при подключённом бэкенде. Список держим здесь, чтобы он не расползался по комментариям:
 *
 * - категория и состояние вещи — полей нет в схеме `Item`;
 * - желание списком вариантов — в контракте одна строка `wantDescription`;
 * - отмена варианта из-за ушедшей вещи (`cancelled`) — статуса нет: конкурирующие цепочки
 *   бэкенд отклоняет как `REJECTED`, а причину кладёт в событие;
 * - уведомления — эндпоинтов нет, поток событий SSE описан без типов событий;
 * - служебный канал «Авито Обмен» в переписке — это пояснение интерфейса, а не разговор:
 *   отправитель сообщения в контракте всегда пользователь;
 * - подсказки желания и распознавание фото — эндпоинтов нет.
 *
 * Стадия передачи из списка ушла: в 0.6.0 появились `POST /chains/{id}/receipt` и статус
 * `COMPLETED`. ⚠️ Но кто именно отметил получение, в цепочке не приходит — значит прогресс
 * «N из M» на реальных данных не показать, признак есть только у закрытой цепочки целиком.
 */
export const MOCKED_ANYWAY = [
  'item.category',
  'item.condition',
  'item.wish[]',
  'chain.cancelled',
  'messages.service',
  'notifications',
] as const
